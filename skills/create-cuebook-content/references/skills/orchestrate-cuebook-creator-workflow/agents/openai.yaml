interface:
  display_name: "Orchestrate Cuebook Creator Workflow"
  short_description: "Run Cuebook's Frame-only creator workflow"
  default_prompt: "Resolve this Frame ContentRecipeV1 against the pinned SkillCatalogV1. Preserve the creator's viewpoint, fetch only material support, then batch body, visual, and optional settlement branches. Return three finished choices whose only public projection is title, body, and one paired image."
