interface:
  display_name: "Query Cuebook"
  short_description: "Search and inspect Cuebook data without creating content"
  default_prompt: "Use one evidence plan to reconcile Cuebook reads with a bounded authoritative Web lane for material current public claims. For a creator handoff, preserve raw candle and market-state envelopes and optionally return at most one aligned and one contrasting or adjacent Cue as viewpoint context, never as proof, consensus, or adopted creator stance. Keep the observed history window separate from any future thesis horizon, label assistant-chosen proxies explicitly, retain lineage internally, and return a sourced read-only CuebookQueryBundleV1 without drafting or publishing content."
