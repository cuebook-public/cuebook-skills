interface:
  display_name: "Query Cuebook"
  short_description: "Inspect Cuebook data and optional local TradingView context"
  default_prompt: "Use one evidence plan to reconcile Cuebook reads with a bounded authoritative Web lane for material current public claims. Only when I explicitly ask and the connector is already present, add one bounded TradingView observation, focused latest-structure chart capture, or network research check. Preserve exact asset identity, source lineage, and raw Cuebook candle envelopes; never adopt provider recommendations, mutate my chart, or pass a raw TradingView capture into Frame creation. Return a sourced read-only CuebookQueryBundleV1 without drafting or publishing content."
