interface:
  display_name: "Create With Cuebook"
  short_description: "Turn a trading idea into title, body, and one paired Frame image"
  default_prompt: "Create one best Cuebook Frame fast preview from this trading idea: one title, one concise body, and one paired image. Recover subject, direction, the historical observation window, and the separate future horizon. Reflect the distinctive kernel, then ask one optional high-leverage heuristic question; if I say continue, immediately run one small Cuebook-first batch. Route price, trend, relative-strength, volume, or horizon claims to one sourced market chart, and qualitative mechanism or intuition to one logic card. Let the evaluated claim choose the axis: a future higher-price claim stays single-price even when a benchmark is context, while a relative axis is reserved for an outperformance claim. Keep observed data distinct from my causal hypothesis, preserve and elevate my viewpoint, and use the single fast-preview runner instead of reconstructing the workflow. Show the preview before release packaging; generate three only if I explicitly request alternatives."
