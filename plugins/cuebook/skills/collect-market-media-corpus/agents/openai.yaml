interface:
  display_name: "Collect Market Media Corpus"
  short_description: "Normalize authorized multimodal market samples"
  default_prompt: "Use $collect-market-media-corpus to normalize these public or authorized articles, threads, image notes, or videos into MediaCorpusV1."
policy:
  allow_implicit_invocation: false
