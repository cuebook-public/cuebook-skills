interface:
  display_name: "Assemble Frame Candidates"
  short_description: "Build three Cuebook Frames with title, body, and paired image"
  default_prompt: "Use the cached fast path to generate three concise Cuebook Frame candidates. Preserve the creator's viewpoint, attach typed evidence and optional settlement only as backstage metadata, pair each candidate with its launch-linted visual, and expose exactly title, body, and one image in the frontend projection."
